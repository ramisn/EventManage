<div class="row">
  <ul class="tabs">
    <li><%= link_to 'Matches', matches_event_path(@event.slug) %> </li>
    <li><%= link_to 'Teams', teams_event_path(@event.slug) %> </li>
    <li><%= link_to 'Groups', groups_event_path(@event.slug) %> </li>
    <li><%= link_to 'Results', results_event_path(@event.slug) %> </li>
    <li><%= link_to 'Rules', rules_event_path(@event.slug) %></li>
  </ul>
</div>